import { BotonMeGustaPipe } from './boton-me-gusta.pipe';

describe('BotonMeGustaPipe', () => {
  it('create an instance', () => {
    const pipe = new BotonMeGustaPipe();
    expect(pipe).toBeTruthy();
  });
});
