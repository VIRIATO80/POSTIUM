import { BotonMeGustaPipePipe } from './boton-me-gusta-pipe.pipe';

describe('BotonMeGustaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BotonMeGustaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
